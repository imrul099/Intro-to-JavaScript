/**
 * 8 Ways to get Undefined.
 * 1. variable that is not initialized will give undefined.
 * 2. fuction with no return.
 * 3.parameter that is not passed will be undefined.
 * 4. if return has nothing on the right side will return undefined.
 * 5. property that doesn't exists on an objects will give you undefined.
 * 6. accessing array elements outside of the index range will give you undefined.
 *7. deleting an element inside an array.[shouln't use delete, use slice];
 *8. set a value directly to undefined.[shouln't use]
 */